spring:
  datasource:
    url: jdbc:mysql://localhost:3306/{veritabanı adı}?serverTimezone=Europe%2FIstanbul
    username: {kullanici adı}
    password: {şifreniz}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
